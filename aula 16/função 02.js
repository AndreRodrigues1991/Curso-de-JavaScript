function soma(n1, n2) { // a função soma vai receber dois valores 
    return n1 + n2
}

console.log(soma(2, 5)) // A soma entre 2 e 5

// vai mostrar o 7 na tela pq o parâmetro n1 é 2 e o n2 é 5

// soma(n1=0, n2=0) parâmetros opcionais so quando a soma so tiver um parametro ai o segundo vai valer 0 pra somar ficando assim 2 + 0 -- 2